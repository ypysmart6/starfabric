global:
  scrape_interval: 5s
  evaluation_interval: 5s
rule_files:
  - /etc/prometheus/rules/*.yml
scrape_configs:
  - job_name: starfabric-controller
    static_configs:
      - targets: ["controller:8081"]
    metrics_path: /metrics
