! RFC 9552 BGP-LS export from the existing IS-IS topology to an SDN collector.
! Replace the peer address and AS with the supplied collector testbed.
router bgp 65000
 neighbor 192.0.2.100 remote-as 65001
 address-family link-state
  neighbor 192.0.2.100 activate
 exit-address-family
