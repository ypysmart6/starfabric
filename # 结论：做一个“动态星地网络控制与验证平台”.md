# 结论：做一个“动态星地网络控制与验证平台”

> **文档状态：历史设计输入。** 当前工程以 `README.md`、`docs/architecture.md`、
> `docs/coverage.md` 和可机读的 `docs/coverage-manifest.json` 为唯一事实源。
> 本稿中早期 C++/Netlink 主线已升级为 Go + OpenConfig/gNMI/gRIBI/FRR；
> 无标准设备 API 的星上断联自治由 Rust runtime 承担。以下里程碑是设计背景，
> 不能单独用作“已实现”证据。

我完整读完了这个 txt。里面最正确的判断是：**企业并不招聘一个只会提出路由算法的人，而是招聘能够实现控制平面、路由软件、节点 Agent、网络测试平台、可观测性与故障恢复系统的人。**论文决定研究问题，工程能力决定能否把问题做成系统。

txt 中已经把你的主方向收敛到了“动态 LEO 网络 + SDN 控制平面 + 云原生运维”，这个选择适合你已有的卫星通信积累，也能迁移到云网络和运营商网络。

但原方案仍有一个问题：它同时包含轨道仿真、路由算法、C++、Rust、FRR、Kubernetes、eBPF、节点 Agent 和 5G NTN，容易重新变成“技术清单”。

因此，我建议将其收敛为下面这一个产品。

---

# 项目名称

## StarFabric：动态星地网络控制与验证平台

英文可以写成：

**StarFabric — A Deployable Control and Validation Platform for Time-Varying LEO Networks**

一句话定义：

> 输入卫星轨道、链路窗口、链路遥测和业务需求，持续生成版本化网络拓扑，计算并安全下发路由与流量工程策略到真实 Linux/FRR 网络节点，验证实际转发结果，并在链路、节点或控制器故障时自动恢复或回滚。

它不是一个“卫星路由算法仿真程序”，而是一个：

* 动态网络数字孪生平台；
* SDN 控制器；
* Linux 网络节点 Agent；
* 路由策略回归测试系统；
* 故障注入与恢复平台；
* 卫星 NOC 的开发和验证环境。

这才是它真正能被使用的地方。

## 真实用户

第一类用户是你所在实验室的研究人员。他们可以把已有的卫星路由、网关选择或资源调度算法作为插件接入平台，而不需要每个人重新编写仿真器。

第二类用户是网络软件开发人员。他们可以在修改路由策略、控制器或节点软件之后，用固定场景自动执行回归测试。

第三类用户是网络运维或系统验证人员。他们可以重放链路失效、节点失联、遥测延迟、错误路由和控制器重启等事故。

必须明确边界：在没有真实卫星载荷、交换芯片 SDK 和厂商遥测接口的情况下，它不是实际飞行软件；但它可以成为一个真实可用的**部署前验证、软件在环和半实物网络测试平台**。这比声称“做了一个卫星互联网系统”更可信。

---

# 为什么这个项目符合当前需求

截至 2026 年 8 月，Amazon Leo 当前公开的 SDN 控制平面岗位仍直接要求路由与转发、流量工程、高可用控制平面、自动化测试和运维能力，并明确涉及 C 和 Rust。其光学星间链路网络软件岗位进一步要求把控制器路由转换成转发表、开发嵌入式 Linux 网络软件，并掌握 BFD、MPLS、ARP、抓包、结构化日志和指标。([Amazon.jobs][1])

这意味着你最应该证明的不是“我设计过一个新的最短路算法”，而是：

> 我能让动态拓扑进入控制器，让控制器生成安全路由，让路由进入真实 FIB，让真实数据包转发，并证明系统在故障和错误输入下仍然可靠。

---

# 项目的最终使用方式

项目完成后，使用者应该能够执行类似下面的操作：

```bash
# 启动一个24颗卫星、4个网关的动态网络
sfctl lab up scenarios/leo-24x4.yaml

# 启动业务流
sfctl traffic apply traffic/mixed-services.yaml

# 运行分布式IS-IS基线
sfctl experiment run --policy isis-bfd

# 运行中央控制器策略
sfctl experiment run --policy centralized-te

# 制造星间链路故障
sfctl fault link-down sat-07 sat-08 --duration 30s

# 杀死主控制器
sfctl fault process-kill controller-0

# 比较两个方案
sfctl experiment compare run-001 run-002

# 输出性能和故障报告
sfctl report export run-002 --format html
```

最终报告至少包括：

* P50、P95、P99 端到端时延；
* 故障期间丢包；
* 故障发现时间；
* 路径计算时间；
* 路由下发时间；
* 拓扑变化到 FIB 生效时间；
* 路由震荡次数；
* 链路利用率；
* 控制器 CPU 和内存；
* 控制器重启恢复时间；
* 期望路由和实际 FIB 的不一致时间。

txt 中提出的旗舰项目架构已经包含轨道服务、动态拓扑、路径引擎、控制器、节点 Agent 和遥测系统；这里需要做的，是把它从概念图转变为有用户流程、接口、测试和验收标准的产品。

---

# 总体架构

```text
                  ┌──────────────────────────────┐
                  │ Scenario / Ephemeris Engine  │
                  │ TLE、轨道、链路窗口、故障脚本   │
                  └──────────────┬───────────────┘
                                 │ Topology Events
                                 ▼
                  ┌──────────────────────────────┐
                  │ Versioned Topology Service   │
                  │ 拓扑版本、链路状态、有效时间窗   │
                  └──────────────┬───────────────┘
                                 │
   Traffic Intent ───────────────┤
                                 ▼
                  ┌──────────────────────────────┐
                  │ Path / Traffic Engineering   │
                  │ Dijkstra、ECMP、约束路径、备份路径│
                  └──────────────┬───────────────┘
                                 │ RoutePlan vN
                                 ▼
                  ┌──────────────────────────────┐
                  │ Controller / Reconciler      │
                  │ 校验、分批下发、提交、回滚、对账  │
                  └──────────────┬───────────────┘
                                 │ gRPC / mTLS
               ┌─────────────────┼──────────────────┐
               ▼                 ▼                  ▼
       ┌──────────────┐  ┌──────────────┐   ┌──────────────┐
       │ Node Agent 1 │  │ Node Agent 2 │   │ Node Agent N │
       │ C++ / Netlink│  │ C++ / Netlink│   │ C++ / Netlink│
       └──────┬───────┘  └──────┬───────┘   └──────┬───────┘
              ▼                 ▼                  ▼
       Linux FIB / FRR   Linux FIB / FRR    Linux FIB / FRR
              │                 │                  │
              └────────────── Real Packets ───────┘
                                 │
                                 ▼
                  ┌──────────────────────────────┐
                  │ Telemetry / NOC / Replay     │
                  │ Logs、Metrics、Traces、pcap   │
                  └──────────────────────────────┘
```

---

# 核心模块设计

## 1. Scenario Engine：动态场景与链路调度器

**语言：Python**

职责：

* 读取 TLE、预计算星历或者自定义 CSV；
* 生成节点位置和链路可见窗口；
* 根据时间推进产生链路 Up/Down 事件；
* 修改链路时延、带宽、丢包和抖动；
* 注入节点失效、链路退化、网关过载等事件；
* 支持确定性重放。

第一版不需要追求精密轨道动力学。它的主要作用是生成**可重复的时变网络事件**。

每次运行都必须记录：

```text
scenario_id
random_seed
input_version
event_sequence
start_time
software_commit
configuration_digest
```

否则无法进行真正的回归比较。

---

## 2. Route Agent：真实 Linux 网络节点软件

**语言：C++20**

这是整个项目最先实现、也最重要的模块。

职责：

* 通过 Netlink 读取接口、邻居表和路由表；
* 添加、删除和替换 IPv4/IPv6 路由；
* 上报实际 RIB/FIB 状态；
* 执行接口 Up/Down；
* 上报链路状态和节点心跳；
* 支持配置版本；
* 所有操作保持幂等；
* 控制器失联后进入本地安全模式；
* 暴露健康检查、日志、指标和调试接口。

例如控制器重复下发同一个 `RoutePlan`，Agent 不得重复修改内核路由，也不能因为消息重试产生错误。

第一版直接操作 Linux FIB。第二版再与 FRR、SRv6 或交换机抽象层集成。

---

## 3. Versioned Topology Service：版本化拓扑状态

第一版可以直接放在 C++ 控制器进程中，不要过早拆微服务。

核心数据结构：

```text
TopologySnapshot
  version
  generated_at
  valid_from
  valid_until
  nodes[]
  links[]

Link
  source
  destination
  admin_state
  operational_state
  latency_us
  capacity_bps
  loss_rate
  reliability
  valid_until
  telemetry_timestamp
```

它必须正确处理：

* 重复事件；
* 乱序事件；
* 延迟遥测；
* 过期遥测；
* 部分节点失联；
* 拓扑快照和增量事件并存；
* 控制器重启后的状态恢复。

不能把“收到的最后一条消息”简单当成真实状态。

---

## 4. Path Engine：路径与流量工程引擎

**语言：C++20，独立静态库**

第一版只实现成熟基线：

* Dijkstra；
* ECMP；
* K 条候选路径；
* 链路不相交备份路径；
* 节点不相交备份路径；
* 延迟约束路径；
* 容量约束路径；
* 网关选择。

不要在第一版加入强化学习、图神经网络或所谓智能路由。

你的已有论文算法以后以插件形式接入：

```text
PathPolicy
  ├── DijkstraPolicy
  ├── ECMPPolicy
  ├── DisjointPathPolicy
  ├── ConstraintAwarePolicy
  └── YourResearchPolicy
```

所有策略使用相同输入、输出和测试场景，这样论文算法才能与真实基线公平比较。

---

## 5. Controller / Reconciler：闭环控制器

控制器不能采用“发出一次命令就认为成功”的设计。

它维护：

```text
Desired State
    ↓
Route Plan
    ↓
Agent Apply Result
    ↓
Actual FIB State
    ↓
Data-plane Probe Result
    ↓
Reconciliation
```

OpenConfig/gNMI 本身就是面向配置和流式遥测的网络管理接口，适合作为后期 Agent 管理适配层；OpenTelemetry Collector 则适合统一接收、处理和导出日志、指标与调用链。([openconfig.net][3])

### 每次路由变更应经过八个步骤

1. 锁定拓扑版本；
2. 计算 RoutePlan；
3. 检查下一跳是否存在；
4. 检查环路、黑洞和链路过期；
5. 向节点发送 Prepare；
6. 小范围或入口节点先行应用；
7. 验证 FIB 和探测流量；
8. Commit，或者自动 Rollback。

RoutePlan 应至少包含：

```text
plan_id
topology_version
created_at
expires_at
routes[]
preconditions[]
rollback_plan_id
```

这比“控制器调用一个 API 下发路由”更接近真实生产系统。

---

## 6. 真实数据平面

实验环境使用：

* Linux network namespace；
* veth；
* containerlab；
* FRRouting；
* `tc netem`；
* iperf3；
* tcpdump/Wireshark。

FRR 的职责就是进行路由决策并将结果安装到操作系统内核；containerlab 官方也提供可重复部署的 FRR 网络实验。([docs.frrouting.org][4])

项目保留两种运行模式：

| 模式      | 作用                            |
| ------- | ----------------------------- |
| 分布式基线模式 | FRR IS-IS/OSPF + BFD 自主收敛     |
| 中央控制模式  | StarFabric Controller 计算并下发路由 |
| 混合模式    | 分布式路由保障基本连通，中央控制器下发 TE 策略     |

BFD 用于快速发现双向转发路径故障；后期可以加入 SRv6 或 MPLS Segment Routing 流量工程。FRR 当前官方文档包含 BFD、SRv6，以及由中央控制器驱动的 Segment Routing Policy 能力。([docs.frrouting.org][5])

---

## 7. 可观测性与 NOC

第一版监控：

| 类别  | 指标                          |
| --- | --------------------------- |
| 拓扑  | 活跃节点数、链路数、过期遥测数             |
| 控制器 | 路径计算时间、队列长度、重试次数            |
| 路由  | RoutePlan 版本、下发成功率、FIB 不一致数 |
| 网络  | RTT、丢包、吞吐、链路利用率             |
| 故障  | 检测时间、收敛时间、恢复时间              |
| 服务  | 请求延迟、错误率、CPU、内存             |
| 安全  | 未授权操作、策略拒绝、回滚次数             |

日志必须带有：

```text
trace_id
scenario_id
topology_version
plan_id
node_id
operation_id
```

不能只输出：

```text
route install failed
```

而应该输出：

```text
plan_id=184
node_id=sat-07
prefix=2001:db8:42::/64
next_hop=2001:db8:7:8::2
kernel_errno=101
error="network unreachable"
```

在 Kubernetes 阶段，可以加入 Cilium/Hubble。Hubble 当前支持节点、集群以及多集群层面的网络流量可观测性。([Cilium Documentation][6])

---

# 项目严格不做什么

第一版明确排除：

| 暂不做                    | 原因             |
| ---------------------- | -------------- |
| 自研 PHY、调制解调器或 RF 模型    | 与网络系统主线不一致     |
| 直接实现完整 3GPP NTN 协议栈    | 范围过大           |
| 强化学习路由                 | 会掩盖控制系统基本问题    |
| DPDK                   | 还没有数据平面性能瓶颈    |
| 自研前端大屏                 | Grafana 足够     |
| 一开始使用 Kubernetes       | 会掩盖 Linux 网络基础 |
| 同时使用 C++ 和 Rust 重写全部服务 | 学习成本失控         |
| 数千个真实容器节点              | 先分离功能测试与规模仿真   |

**第一版核心语言只使用 C++20 和 Python。**

Rust 放到 v1.1：选择拓扑服务、遥测采集器或控制器中的一个服务进行实现，而不是双语言重复造轮子。这遵循 txt 中“C++20 第一主语言、Rust 第二系统语言、Python 自动化语言”的顺序。

---

# 24 周实施路线

## Release 0.1：Linux Route Agent

### 第 1—2 周

交付：

* C++20 工程模板；
* CMake；
* GoogleTest；
* clang-format、clang-tidy；
* AddressSanitizer、UndefinedBehaviorSanitizer；
* Docker 构建；
* CI；
* Netlink 接口和路由读取；
* Netlink 路由添加、删除、替换；
* 三个 namespace 的集成测试。

验收：

1. 能读取真实 Linux 路由表；
2. 能向指定 namespace 写入路由；
3. 相同命令执行两次不产生副作用；
4. 非法下一跳被明确拒绝；
5. Agent 重启后能够重新读取实际状态；
6. 单元测试和 namespace 集成测试自动运行。

---

## Release 0.2：可重复动态网络实验

### 第 3—6 周

交付：

* containerlab 拓扑；
* 12—24 个 FRR/Linux 节点；
* IS-IS 或 OSPF 基线；
* BFD；
* Scenario Engine；
* 动态链路 Up/Down；
* 动态时延、带宽和丢包；
* iperf3 和持续探测流量；
* 故障过程自动抓包。

必须完成的场景：

* 单链路失效；
* 单节点失效；
* 链路时延突增；
* 链路容量下降；
* 网关失效；
* 连续链路切换；
* 路由震荡；
* BFD 与普通路由定时器对比。

验收：

* 相同场景和随机种子产生相同事件序列；
* 能输出故障前、故障中、故障后的时延和丢包；
* 能自动记录 FRR 路由表变化；
* 实验完全由一个命令启动和结束。

---

## Release 0.3：中央控制器闭环

### 第 7—10 周

交付：

* 版本化拓扑；
* Dijkstra 和 ECMP；
* gRPC 控制接口；
* RoutePlan；
* Controller/Reconciler；
* Node Agent 状态上报；
* 真实 Linux FIB 下发；
* FIB 一致性检查；
* 中央控制与 FRR 基线对比。

验收场景：

```text
拓扑 v100
    ↓
控制器生成 RoutePlan p203
    ↓
24 个 Agent 接收
    ↓
22 个成功，2 个暂时失败
    ↓
控制器重试或回滚
    ↓
最终 Desired State = Actual State
```

起始性能预算：

* 24 个真实网络节点；
* 100 条持续业务流；
* 每秒至少一次拓扑变化；
* 路径计算 P95 小于 200 ms；
* 拓扑变化到 FIB 生效 P95 小于 2 s；
* 单链路故障后不允许出现持续性环路或黑洞；
* 重复、乱序拓扑事件不得破坏状态。

这些数字是第一版工程预算，不是论文结果。运行后应根据固定测试机建立正式回归基线。

---

## Release 0.4：可靠性与生产化

### 第 11—14 周

交付：

* 控制器状态持久化；
* 超时、重试和指数退避；
* 优雅退出；
* 心跳和租约；
* 路由计划过期；
* 分批下发；
* 自动回滚；
* 结构化日志；
* Prometheus 指标；
* OpenTelemetry traces；
* 故障注入测试；
* 24 小时稳定性运行。

必须注入：

| 故障       | 正确行为         |
| -------- | ------------ |
| 控制器进程崩溃  | 重启后恢复拓扑和计划状态 |
| Agent 失联 | 标记不可达，不无限重试  |
| 消息重复     | 幂等处理         |
| 消息乱序     | 拒绝旧版本        |
| 拓扑过期     | 不使用已失效链路     |
| 部分路由下发失败 | 回滚或保持安全基线    |
| 遥测错误     | 不直接触发危险操作    |
| 日志系统不可用  | 核心控制不能阻塞     |

txt 对生产级代码的定义包括可重复构建、静态分析、单元测试、集成测试、端到端测试、性能测试、故障注入、健康检查、日志、指标、Tracing、重试、优雅退出、拓扑版本、幂等下发、部分失败处理和回滚。这个清单应直接成为项目的 Definition of Done。

---

## Release 0.5：前沿流量工程

### 第 15—19 周

这一阶段再加入真正的前沿能力：

### SRv6 流量工程

为不同业务类别选择不同路径：

| 业务    | 策略     |
| ----- | ------ |
| 交互业务  | 最低时延   |
| 视频业务  | 容量优先   |
| 控制业务  | 双路径冗余  |
| 批处理业务 | 避开拥塞链路 |

实现：

* 入口节点 SRv6 Policy；
* 显式路径；
* 备份 Segment List；
* 策略版本；
* Canary 下发；
* 失败回滚。

### Shadow Control

新策略不立即进入真实网络，而是：

1. 接收同一份实时拓扑；
2. 在影子控制器中计算；
3. 与当前策略比较；
4. 生成“假如下发”的性能预测；
5. 通过验证后再进入 Canary。

### eBPF 数据平面验证

eBPF 不用于第一版替代整个转发系统，而用于：

* 流量采样；
* 路径延迟测量；
* 丢包原因观测；
* 数据平面探测；
* 识别控制平面与实际流量不一致。

---

## Release 1.0：可部署网络控制产品

### 第 20—24 周

交付：

* Docker Compose 单机部署；
* Kubernetes 部署；
* 控制器多副本；
* Leader Election；
* 健康检查和自动重启；
* 配置版本；
* mTLS；
* RBAC；
* 审计日志；
* 灰度升级；
* 回滚；
* Grafana NOC Dashboard；
* 一键运行的演示环境；
* 一份完整性能报告；
* 一份运维 Runbook。

Kubernetes 的生产部署通常使用多个控制平面实例，以获得故障容错和高可用能力；这一阶段的重点不是学会执行 Helm 命令，而是验证服务重启、Leader 切换、配置升级和状态恢复。([Kubernetes][7])

---

# 自动化边界

本项目只保留确定性控制、规则驱动验证、显式 Runbook 和经审批的操作。
任何路由变更都必须经过版本、权限、dry-run、shadow、canary、数据面验证和回滚门禁。

---

# 5G NTN 如何接入，而不把项目做散

StarFabric 1.0 完成后，再增加一个 **5G NTN Transport Adapter**。

拓扑为：

```text
UE / UE Emulator
        ↓
srsRAN gNB
        ↓ N2 / N3
StarFabric Dynamic Satellite Transport
        ↓
Open5GS Core
        ↓
Application Server
```

这时研究的不是 PHY 算法，而是实际系统问题：

* 动态卫星传输对注册流程的影响；
* N2/N3 中断；
* 网关切换；
* QoS 和业务连续性；
* GTP-U 流量路径；
* TCP/QUIC 在链路切换时的行为；
* 控制面恢复与用户面恢复的差异。

截至 2026 年，3GPP Release 19 已于 2025 年 12 月完成，Release 20 正在覆盖进一步的 5G-Advanced 工作和首批 6G 研究；srsRAN 官方也提供 5G SA NTN 配置教程。因此，将 NTN 作为 StarFabric 的后续真实业务接入层仍然具有现实性。([3gpp.org][8])

但它必须是 1.0 之后的扩展，而不是第一天的工作。

---

# 仓库结构

```text
starfabric/
├── CMakeLists.txt
├── proto/
│   ├── topology.proto
│   ├── route_plan.proto
│   ├── node_agent.proto
│   └── telemetry.proto
├── controller/
│   ├── topology/
│   ├── reconciler/
│   ├── persistence/
│   └── api/
├── path-engine/
│   ├── graph/
│   ├── policies/
│   └── validation/
├── route-agent/
│   ├── netlink/
│   ├── fib/
│   ├── telemetry/
│   └── watchdog/
├── scenario/
│   ├── ephemeris/
│   ├── scheduler/
│   └── fault_injection/
├── cli/
│   └── sfctl/
├── lab/
│   ├── containerlab/
│   ├── frr/
│   ├── traffic/
│   └── scenarios/
├── observability/
│   ├── opentelemetry/
│   ├── prometheus/
│   ├── grafana/
│   └── alerts/
├── deployments/
│   ├── compose/
│   └── kubernetes/
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── e2e/
│   ├── chaos/
│   └── benchmark/
└── docs/
    ├── architecture/
    ├── adr/
    ├── api/
    ├── runbooks/
    └── performance/
```

---

# 第一批 GitHub Issues

你现在不应该继续阅读更多论文。第一批任务直接建立为下面十个 Issue：

| Issue | 内容                                                   |
| ----- | ---------------------------------------------------- |
| #1    | 建立 C++20、CMake、GoogleTest、clang-tidy、sanitizer 工程    |
| #2    | 通过 Netlink 枚举 Linux 接口                               |
| #3    | 通过 Netlink 读取 IPv4/IPv6 路由表                          |
| #4    | 在指定 namespace 中添加和删除路由                               |
| #5    | 为路由操作实现幂等语义                                          |
| #6    | 建立三节点 namespace 网络                                   |
| #7    | 实现 Agent gRPC `GetState`                             |
| #8    | 实现 Agent gRPC `ApplyRoutePlan`                       |
| #9    | 暴露 `route_apply_total`、`route_apply_failed_total` 指标 |
| #10   | 编写“链路断开—路由切换—流量恢复”端到端测试                              |

第一个月的完成标准只有一句话：

> 控制器向一个真实 Linux 网络节点发送版本化路由计划，Agent 通过 Netlink 修改内核 FIB，并通过抓包和持续流量证明数据包确实改变了路径。

这一步没有完成之前，不学习 Kubernetes、不写新的节点 Agent、不接入 NTN。

---

# 最终成果应当是什么

StarFabric 完成后，你应拥有：

1. 一个可公开运行的 C++20 网络控制系统；
2. 一个真实 Linux/FRR 动态网络实验平台；
3. 一套自动故障注入和回归测试；
4. 一套日志、指标、Tracing 和 NOC Dashboard；
5. 一套路径计算、路由下发和 FIB 验证闭环；
6. 一份完整系统设计文档；
7. 一份性能与可靠性报告；
8. 一套部署、升级、回滚和故障处理 Runbook；
9. 一个小型 FRR、containerlab、srsRAN 或相关项目的上游贡献；
10. 一个可以在面试中逐层解释的系统。

届时你的技术定位不是：

> 做卫星路由算法的博士。

而是：

> **能够设计、实现、部署、验证和诊断动态网络控制系统的 Network Systems / SDN / Routing Software Engineer，具备 LEO 与 NTN 领域优势。**

这与 txt 最后的定位一致：卫星通信是领域优势，5G NTN 是行业接口，云原生 NOC 是工程出口。

项目的第一项实际工作应当是：**建立 `route-agent` 仓库，并完成 Netlink 路由表读取。**

[1]: https://www.amazon.jobs/jp/jobs/10493715/software-development-engineer-software-defined-network-control-plane-amazon-leo?utm_source=chatgpt.com "Software Development Engineer , ..."
[3]: https://www.openconfig.net/docs/gnmi/gnmi-specification/?utm_source=chatgpt.com "gRPC Network Management Interface (gNMI) specification"
[4]: https://docs.frrouting.org/en/latest/about.html?utm_source=chatgpt.com "About FRR — FRR latest documentation - FRRouting"
[5]: https://docs.frrouting.org/en/latest/bfd.html?utm_source=chatgpt.com "BFD — FRR latest documentation"
[6]: https://docs.cilium.io/en/stable/overview/intro/?utm_source=chatgpt.com "Introduction to Cilium & Hubble"
[7]: https://kubernetes.io/docs/setup/production-environment/tools/kubeadm/high-availability/?utm_source=chatgpt.com "Creating Highly Available Clusters with kubeadm"
[8]: https://www.3gpp.org/specifications-technologies/releases/release-20?utm_source=chatgpt.com "Release 20"
